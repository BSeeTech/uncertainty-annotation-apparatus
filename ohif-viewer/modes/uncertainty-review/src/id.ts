/**
 * The mode's canonical ID — must match `package.json#name` so that the
 * OHIF extension manager can resolve `@thesis/mode-uncertainty-review`
 * by string lookup.
 */
export const id = '@thesis/mode-uncertainty-review';
